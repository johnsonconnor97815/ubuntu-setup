# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目目标

把一台**全新安装的 Ubuntu 机器**(含 Server / SSH / 无桌面)变成**用脚本集合管理软件**的机器。核心模型是**脚本集合 + 分类管理**:

1. **脚本是所有安装 / 配置 / 管理功能的唯一权威实现。** 每个软件一个 `scripts/<key>.sh`,统一接口(`meta` / `status` / `install` / `remove` / 可选 `configure` / `help`),全部 `source` 共享库 `lib/common.sh`——后者把项目的安全契约写成**可复用、被强制的代码**(幂等探测、逐命令 sudo、非交互 apt、改前备份、保守的 vendor apt 渠道)。写一个合规脚本基本上就是调这些 helper;不安全写法(`sudo npm`、整体 root、`apt-key`)**故意没有原语**。

2. **三个入口调用同一批脚本**:① `bootstrap.sh` 的现代全屏 TUI(`lib/ui.sh` 自绘,**无 whiptail**,非富终端回退纯文本)——它钻进**每个脚本自己的 `ui()` 管理界面**;② 人直接跑脚本 / 薄启动器 `swkit <软件> <操作> [args]`(或 `swkit <软件>` 开该软件界面、`swkit ui` 开总目录);③ LLM 经 skill。三个入口**地位等同**:都只是**调用**脚本。LLM 经 skill **运行并推荐**脚本(挑对的脚本与选项、解释取舍),但**不在运行时编写或演进脚本**——脚本统一在**本仓库**里维护;要新增软件覆盖,由维护者在仓库里加一个脚本(`cp scripts/TEMPLATE.sh`)。仓库交付一组**分类好的脚本**。

3. 这**取代**了旧模型(LLM 凭 skill 散文规则即兴拼装原始安装命令),也**消除了旧的双写问题**(同一软件的配置逻辑既写在 bootstrap 的 bash 里、又活在 skill 散文里)。现在「怎么做」的唯一来源是脚本;skill 散文降级为「脚本必须满足的契约 + 如何用脚本」。

**没有 Python 包、没有数据驱动 catalog 引擎**——那个 Python/YAML 被引擎解释的声明式安装清单是被放弃的旧方向(它要求先有 `python3-yaml`/`uv` 等前置依赖,违背"全新 Ubuntu 即用"的初衷)。脚本是**纯 bash、每软件一个、自硬编码自己的元数据**;TUI 通过读各脚本的 `meta` 子命令**动态发现**并归类——这是「只读脚本自报的有界元数据」,不是被放弃的 YAML 引擎。

**部署**:**没有独立部署副本,kit 原地从 clone 跑**。`bootstrap.sh` 只把 `swkit` 软链到 `~/.local/bin/swkit`(指向 clone 里的 `swkit`),每个脚本经 lib 相对自身定位找到同仓的 `lib/`/其他脚本(`KIT_SCRIPTS_DIR`)——**clone 即唯一来源**。要改脚本直接改 clone、`git pull` 即更新(swkit 直指它们),无任何「部署副本 vs 仓库」双写。clone 放哪即装哪,删了 clone 即卸载。skills 仍需**部署**到 `~/.claude/skills/<name>/`(Claude Code)与 `~/.codex/prompts/<name>.md`(Codex)——这是 Claude Code/Codex 从固定位置读 skill 的要求,重跑 bootstrap 刷新。(`bootstrap.sh` 还会**清理**旧版遗留的 `~/.local/share/ubuntu-setup` 部署副本。)

定位为**开源产品**:脚本会在作者从未见过的陌生人机器上跑 `sudo`,所以信任与安全是一等约束,不是装饰。

## 当前状态

新架构已落地。真实交付物:

- `bootstrap.sh`——**启动器**:补 kit 依赖 → `install_swkit_path` 把 `swkit` 软链到 `~/.local/bin`(指向 clone;kit **原地从 clone 跑**,无部署副本;`remove_legacy_kit_home` 清掉旧版遗留的 `~/.local/share/ubuntu-setup`;`kit_scripts_dir`=`$SCRIPT_DIR/scripts`)→ 部署 skills → 有终端时进 TUI(`run_tui` 用 `ui_pick` 开顶层菜单「安装软件/设置」,`esc/q` 退出;「安装软件」= `ui_catalog "$(kit_scripts_dir)"`,按类别列脚本、标已装、`→`/回车钻进各脚本**自己的 `ui()`**,`esc` 返回、`q` 退出;「设置」= 语言切换 + 免密 sudo 开关,`esc/q` 返回),否则走 headless(`run_headless`,flag:`--only`/`--method`/`--with-node`/`--skip-skills`/`--headless`/`--tui`,只覆盖两个 CLI / Node / skills 的安装)。**额外动作机制**:脚本可在 `meta` 的 `ops=` 里声明 install/remove/configure 之外的动作(如 zsh 的 `install-omz`/`uninstall-omz`/`default-shell`);`kit_dispatch` 把未知 op `<x>` 路由到 `do_<x>`(连字符转下划线)。**`ui` 是入口模式**(类同 `meta`/`status`/`help`,**不进 `meta.ops`**):`kit_dispatch ui` 在有 TTY 时调脚本的 `ui()`(无则 `ui_default_menu` 合成)、无 TTY 时打印「用 swkit <key> <op>」指引退 0。**带参动作**(zsh 的 `add-plugin <名|url>`/`remove-plugin`/`prompt <名>`)经 `kit_dispatch` 由 `swkit`/LLM 调用,**也**在 zsh 的 `ui()` 界面里交互可达(空格勾选、`a` 加 git 插件、提示符子菜单)。
- `lib/common.sh`——共享库,**安全**契约即代码(探测、`sudo_run`、apt、文件、vendor apt 渠道、`npm_global_writable`/`npm_ensure_user_prefix`、`kit_dispatch`);`kit_dispatch` 开头调 `kit_load_lang`(从 `~/.config/ubuntu-setup/config` grep `LANG_CODE` 设 `UI_LANG`,**不 source 用户文件**、认 `SUDO_USER`),让脚本被 `swkit`/直接调用时也按用户语言渲染——此前仅 `bootstrap.sh` 设 `UI_LANG`(与其 `load_config`/`save_lang` 同步,改一处必查另一处);末尾依次 `source lib/cache.sh`、`lib/ui.sh`。
- `lib/cache.sh`——共享库,**目录探测缓存即代码**(性能契约):被 `common.sh` 在 source `ui.sh` **之前** source,故 `ui_catalog`/`swkit list,search`/`bootstrap` 共享一份缓存。`meta` 按脚本文件 mtime **永久缓存**(`kit_meta_cached`/`kit_meta_into`〔零 fork 填全局供 UI 首帧〕),`status` 安装布尔按软 TTL 缓存 + 精准失效(`kit_status_value`/`kit_status_read`/`kit_probe_status`〔设 `KIT_PROBE_ONLY=1`〕/`kit_cache_invalidate`);并行 `kit_cache_warm`〔预热,强制重探〕/`kit_cache_fill`〔只补缺失/过期,供 `swkit list`〕、`kit_cache_clear`。**陈旧只影响显示不影响行为**(真实 op 永远跑脚本本体、其 `status` 闸门现场复检活系统)。认 `SUDO_USER` 解析真实 home、用户态无 sudo、缓存目录 `~/.cache/ubuntu-setup/catalog/` 0700、`KIT_NO_CACHE=1` 完全旁路、`_kit_now`〔printf 内建替 `date`〕与 `${f##*/}`〔替 `basename`〕减 fork。
- `lib/ui.sh`——共享库,**现代 TUI 渲染即代码**(安全契约的视觉孪生):alt-screen 生命周期 + `trap` 还原 + `SIGWINCH`、`ui_pick`/`ui_confirm`/`ui_input`/`ui_notify`/`ui_run`/`ui_catalog`/`ui_default_menu`、`ui_badge`/`ui_header`/`ui_footer`/`ui_row`/`ui_read_key`〔带可选超时供异步轮询〕、i18n `ui_t`。纯 bash+ANSI,无 whiptail;三档终端:富 TTY 全屏渲染、受限 TTY 纯文本编号菜单、无 TTY 静默降级。**`ui_catalog` 用 stale-while-revalidate**(经 `lib/cache.sh`):`_ui_catalog_scan` 从缓存零 fork 瞬时首帧渲染(~4ms 热/~40ms 冷),后台并行 `kit_probe_status` 重探,`ui_read_key 0.15` 超时轮询收割、status 到达即重绘(三态 badge:已装/未装/`◐`探测中),`r` 硬刷新、返回子界面后精准失效该脚本。
- `scripts/*.sh`——脚本集合,种子集:`git` `curl` `zsh` `fonts` `docker` `ghostty` `tmux` `rime` `vscode` `cursor` `node` `go` `python` `java` `android` `claude` `codex` `codegraph` `trellis` `mattpocock-skills`(`docker`、`git`、`zsh`、`ghostty`、`tmux`、`rime`、`go`、`python`、`java`、`android`、`codegraph`、`trellis`、`mattpocock-skills` 另有 `configure`〔`codegraph` 的 `configure`=`codegraph install --yes` 把 MCP server 接入各 agent;`trellis` 的 `configure`=在**当前 git 仓库**跑 `trellis init -u <name>`、per-repo、不在 git work tree 则报错〕,`ghostty` 还有 `default-terminal`、`tmux` 还有 `install-tpm`/`uninstall-tpm`/`update-plugins`、`rime` 还有 `deploy`/`install-rime-ice`/`remove-rime-ice`、`cursor` 还有 `update`〔重取并装最新官方 `.deb`〕、`go` 还有 `tools`/`update-tools`〔+ 参数化 `set-proxy`/`add-tool`/`remove-tool`〕、`python` 还有 `install-uv`/`remove-uv`/`update-uv`/`tools`/`update-tools`/`list-versions`/`upgrade-versions`/`clear-default`〔+ 参数化 `set-index`/`add-tool`/`remove-tool`/`install-version`/`remove-version`/`set-default`〕、`java` 还有〔+ 参数化 `add-version`/`remove-version`/`set-default`/`ensure-java-home`/`home`〕、`android` 还有 `accept-licenses`/`purge`〔+ 参数化 `add-package`/`remove-package`/`add-platform`/`remove-platform`/`set-mirror`〕、`mattpocock-skills` 还有 `update`〔+ 参数化 `add-skill`/`remove-skill`/`set-agents`/`add-agent`/`remove-agent`/`list`〕、`codex` 还有〔+ 参数化 `set-editor`/`clear-editor`:Codex 的 Ctrl+G 外部编辑器读进程环境 `VISUAL`(回退 `EDITOR`)、无 app-only 配置,故经 `go.sh` 受管 rc 行范式写 `export VISUAL/EDITOR` 进 shell rc——**全局**(同时影响 git 等),curated 编辑器带 `--wait`/`-nw` 阻塞标志,拒绝含 `"`/`$`/`` ` ``/`\` 等会在 source 时注入的元字符〕;**每个脚本都手写了自己的 `ui()`**);外加 `scripts/TEMPLATE.sh`(新增脚本时 `cp` 起步,含 `ui()` 注释样例)。各脚本要点:
  - **`fonts.sh`** 是**用户态 Nerd Font 管理器**(curated:`meslolgs` 旗舰 / `jetbrains-mono` / `firacode` / `hack`;装入 `~/.local/share/fonts` + `fc-cache`,**不用 sudo**;`install [name]`/`remove [name]`/`apply [name] [size] [target]`/`configure --font --size --target`;可把偏好保存到 `~/.config/ubuntu-setup/fonts.conf`,并用用户级 gsettings 应用到检测到的 Ptyxis / GNOME Terminal / GNOME 桌面 monospace;SSH 时给客户端终端指引——字形由本地客户端终端渲染,服务器侧应用只对本地显示生效)。`zsh.sh` 选 starship/p10k 时经 `KIT_SCRIPTS_DIR/fonts.sh` **真正安装** MesloLGS NF(取代旧的仅 `log_warn`)。
  - **`ghostty.sh`** 是 **Ghostty 终端的安装+配置管理器**:
    - **渠道**:apt〔26.04+〕→ 社区 `.deb`〔mkasberg/ghostty-ubuntu,按 `${arch}_${VERSION_ID}` 命中 GitHub release,适配 24.04/25.10〕→ snap `--classic`,**择高而用并打印所用渠道**。
    - **`configure`** 管**主题**〔`theme`,数百内置,支持 `dark:…,light:…` 自动明暗,按 `ghostty +list-themes` 宽松校验〕/ **字体**〔`font-family`+`font-size`,curated Nerd Font 经 `KIT_SCRIPTS_DIR/fonts.sh` 自动装〕/ **常用项**〔`background-opacity`、`cursor-style`/blink、`window-padding`、`copy-on-select`、`mouse-hide-while-typing`、`confirm-close-surface`、`shell-integration`〕;偏好存 `~/.config/ubuntu-setup/ghostty.conf`,整体重生成受管 drop-in `~/.config/ghostty/ubuntu-setup`,并经**绝对路径 `config-file=`** 一次性 include 进用户 `~/.config/ghostty/config`——**不覆盖用户内容**、改前 `backup_file`、`config-file` 用绝对路径绕开相对解析与「cycle detected」。
    - **`default-terminal`〔`off` 反转〕**把 Ghostty 设为默认终端:主力是用户态写 freedesktop **`xdg-terminal-exec`** 选择器(Ghostty 的 `.desktop` id 作 `~/.config/<desktop>-xdg-terminals.list` 首行、保留并去重其他终端,GNOME 25.04+ 的 Ctrl+Alt+T/「Open Terminal」走此),best-effort 再经 `sudo update-alternatives` 注册系统 **`x-terminal-emulator`**(Ghostty 不自注册故先 `--install` 再 `--set`;**仅 .deb/apt**,snap 跳过——存不了 `--gtk-single-instance`;失败/`RC_NEED_SUDO` 仅 `log_warn` 不中断);**故意不碰** legacy gsettings(25.04+ 会倒退现代链)/`$TERMINAL`。
    - **SSH**:Ghostty 是桌面 GUI 终端,SSH/headless 下这些设置作用于**有显示器的机器**,install/configure/default-terminal 会据 `SSH_*` 如实提示。
  - **`tmux.sh`** 是 **tmux 的安装+组件管理器**(渠道 apt;**像 `zsh.sh` 一样按组件管理**——TPM(Tmux Plugin Manager)、curated 插件、主题、常用 options 各自独立增删,状态存 `~/.config/ubuntu-setup/tmux.conf`):
    - **受管块**:每次改动**整体重生成 `~/.tmux.conf` 内的受管标记块**(`# >>> ubuntu-setup tmux (managed block) >>>` … `<<<`;改前 `backup_file`、保留标记外的用户内容)——之所以用「主配置里的标记块」而非 zsh/ghostty 的「单独 drop-in + 一行 include」,是因为 **TPM 只从主配置文件 grep `@plugin` 行、不跟随 `source-file`**,故 `@plugin` 声明与末尾 `run '…/tpm'` 必须落在 `~/.tmux.conf` 本体。
    - **块内容**:**最佳实践 options**(`mouse`/`mode-keys`〔vi/emacs〕/`history-limit`/`escape-time`/`base-index`+`pane-base-index`/`renumber-windows`/`focus-events`/`aggressive-resize`/`set-clipboard`〔OSC52〕/`monitor-activity`/`set-titles`/`status-position`+`status-interval`/RGB `terminal-overrides`/reload bind/可选 prefix 重映射/可选**人体工学键位**〔`\`/`|` 左右分屏、`-`/`_` 上下分屏(均带 cwd)、vim `hjkl` 切窗格、`HJKL` 调大小、copy-mode-vi `v`/`C-v`/`y`;**职责分离:启用 pain-control 时让出 `hjkl`/`HJKL`/`c`(它带 cwd 提供同样键、且 `\`/`_` 为 full-size)、启用 yank 时让出 `y`/MouseDrag——避免与「`run tpm` 后 source、本会覆盖 kit」的插件重复绑定;split 键与 `v`/`C-v` 始终保留(护 `--split-h/-v`、yank 不绑 selection)〕/可选**窗口切换键**〔`Alt+1..9` 跳窗口、`Shift+←/→` 前后窗口〕/可配置的 **copy-mode 入口键**与**窗口树(`choose-tree`)键**〔修饰键 chord 如 `C-Space`/`C-Enter` 绑为**无 prefix 直达**、普通键绑在 prefix 下;用到 chord 时自动开 `extended-keys`〕/**可配置分屏键**〔默认 `\`/`|`(左右)+`-`/`_`(上下),即同一物理键的非 Shift/Shift 态;`--split-h`/`--split-v` 接受空格分隔的多键、各键单引号包裹安全绑定字面 `\`〕/可选 **pane 管理键组**〔`--pane-keys`:`S` 切换同步输入(装 sessionist 时其默认 `S`=switch-client 会抢键,故在 `run tpm` **后**重绑、alternate-session 改用内置 `prefix L`)、`b` break-pane;swap-pane 改用内置 `prefix {`/`}`(原 `>`/`<` 与 pain-control 的 swap-window 冲突,已删)〕/可选 **无 prefix Alt 分屏**〔`--alt-split`,默认关、会抢 `Alt+Enter`/`Alt+方向`,可能与终端/shell/(n)vim 冲突〕)+ `@plugin` 声明(**顺序:主题在前、其余居中、`continuum` 最后**——continuum 靠 status-right hook 自动保存,主题会覆盖 status-right 故 continuum 必须最后 source,否则 autosave 失效)+ 插件变量(`@resurrect-*`〔含可选 `@resurrect-strategy-vim/nvim 'session'`=`--resurrect-strategy`〕/`@continuum-*`〔含可选 `@continuum-boot`〕/各主题 flavor `@catppuccin_flavor`/`@tmux-gruvbox`/`@tokyo-night-tmux_theme`/`@themepack`)+ **状态栏组件**(prefix-highlight/mode-indicator/net-speed/online-status/cpu/battery,占位符串经共享 helper `_tmux_status_widgets_str` 两路共用):**无主题**时由 kit 接管 `status-right`,在 `run tpm` **前**写入占位符,tpm source 各插件时就地插值;**有主题**时主题在 `run tpm` 期间接管 `status-right`,故改为在 `run '…/tpm'` **之后**(主题已写完)`set -ag status-right` 追加占位符、再 `run` **重新 source** 每个启用的 widget 插件(入口文件 = 仓库名去 `tmux-` 前缀、`-`→`_`)就地插值——主题原有内容与 continuum 的 autosave 钩子(prepend 在 `status-right` 最前)都保留,组件不重复,故 **cpu/网速等组件在任何主题下都可见**(此前有主题时只加载不显示,是 bug)+ **resurrect `last` 启动守卫**〔启用 continuum 时,在 `run '…/tpm'`(tpm source continuum 后台 restore 的触发点)**之前**写一条 `run-shell`:resurrect 的 `last` 软链为空/悬空时,删掉 0 字节存档并把 `last` 重指向最近的非空存档(无则删 `last`),否则 no-op——治 tmux-resurrect#287/#403 的「空 `last` 致 `tmux` 启动即 `[exited]`」;空 `last` 的主来源是 continuum-boot 的 systemd `ExecStop` 在 session 全关后保存空布局,故 `--recommended` 也已默认关 boot,但守卫对崩溃/关机中断保存等其他来源同样兜底〕 + 自愈安装片段 + `run '…/tpm'`(其后跟「有主题时的 widget 叠加」行 + **post-TPM kit overrides**〔重夺被插件覆盖的少数键/设置:装 sessionist 时重绑 `prefix S`=synchronize-panes、装 sensible 时重设 `aggressive-resize`=kit 值(sensible 无 user-default 检测会强制 on,使 UI 与实际不符);均依赖 tpm 同步 source〕)。
    - **i18n / 驱动**:**curated 插件与主题的一行说明经 `UI_LANG`(en/zh/ja)本地化**(本地表 `TMUX_I18N`,同 `ui_t` 结构;**名称不译**,符合项目规则)。插件装/更新/清理经 TPM 的 `bin/install_plugins`/`update_plugins`/`clean_plugins` **无 TTY 驱动**(无需交互式 `prefix+I`)。
    - **动作**:`install-tpm`/`uninstall-tpm`(卸载只去掉受管块的插件段、保留插件克隆与状态里的选择)、`update-plugins`(no-arg,进 `meta.ops`);带参 `add-plugin <名|owner/repo[#branch]|git-url>`(curated:`sensible resurrect continuum sessionist yank pain-control vim-navigator open fzf logging battery cpu prefix-highlight mode-indicator online-status net-speed`〔均零依赖;mode-indicator/online-status/net-speed 为状态栏组件〕,其余按 git 仓克隆)、`remove-plugin <名>`、`theme <none|catppuccin|dracula|themepack|gruvbox|tokyo-night> [flavor]`(新增 gruvbox=`egel/tmux-gruvbox`〔`@tmux-gruvbox` dark/dark256/light/light256〕、tokyo-night=`janoamaral/tokyo-night-tmux`〔`@tokyo-night-tmux_theme` night/storm/day〕;flavor 变量名经 `_tmux_theme_flavor_opt` 映射,切主题时 `_tmux_normalize_flavor` 把残留 flavor 归一为新主题默认;需图标的主题经 `fonts.sh` 装 MesloLGS NF)(经 kit_dispatch/swkit/LLM,且在 `ui()` 里交互可达:空格勾插件、`a` 加任意仓、主题/key-mode/prefix 子菜单、「Apply recommended setup」一键装 TPM+常用插件+catppuccin)。
    - **`configure`** 把每个设置暴露为 flag(`--recommended`/`--theme`/`--theme-flavor`/`--status-position`/`--status-interval`/`--mouse`/`--clipboard`/`--focus-events`/`--aggressive-resize`/`--renumber`/`--base-index`/`--monitor-activity`/`--set-titles`/`--keymode`/`--prefix`/`--keybindings`/`--keys-preset`/`--window-nav`/`--copy-mode-key`/`--tree-key`/`--split-h`/`--split-v`/`--pane-keys`/`--alt-split`/`--resurrect-strategy`/`--continuum-boot`/`--history`/`--escape-time`/`--scroll-speed`〔鼠标滚轮每格滚动行数,`default`=tmux 内置 5 行,改值时重绑 `copy-mode`/`copy-mode-vi` 两表的 `WheelUp/DownPane` 的 `send -X -N <n>`,仅 mouse on 时生效〕/`--plugins`/`--no-plugins`),**无参=保守最佳实践基线**(mouse on、vi 复制、true color、50k 回滚、escape-time 10、1-based;不装插件/主题/TPM、不重映射键位),`--recommended` 一键上常用方案(TPM+常用插件+catppuccin+人体工学键位+pane 管理键+会话自动化〔resurrect 策略 + continuum restore;**continuum-boot 不在 --recommended 内、保持 opt-in**——其 systemd `ExecStop` 在 session 全关后保存空布局会损坏 resurrect 的 `last`、致 `tmux` 启动即 `[exited]`(tmux-resurrect#287),见受管块的「启动守卫」〕);另有 **`--keys-preset`**(经辅助函数 `_tmux_apply_keys_preset`,也被 `--recommended` 折叠复用)= **仅键位**的快捷预设〔一键开 `--keybindings`+`--window-nav`+`--pane-keys` 并把分屏键重置为双键 `\`/`|`+`-`/`_`,不碰插件/主题/prefix/keymode〕,UI 的 Keys 区有对应「Apply recommended keybindings」行。**所有设置项在 `ui()` 里都做成快捷 开关/选择器/输入**(Appearance/Behavior/Keys/History 分组 + 视口滚动)。
    - **SSH**:tmux 是多路复用器(非 GUI),SSH/headless 完美适用、无「仅桌面」提示;改动经 `tmux source-file ~/.tmux.conf` 或下次启动生效。
  - **`rime.sh`** 是 **RIME 输入法的安装+组件管理器**(框架定为 **fcitx5**——CJK 体验最好、社区主流;**像 `zsh.sh`/`tmux.sh` 一样按组件管理**:输入方案(schema)、候选词个数、模糊音、雾凇拼音各自独立增删,偏好存 `~/.config/ubuntu-setup/rime.conf`):
    - **渠道(纯 apt)**:核心 `fcitx5 fcitx5-rime fcitx5-config-qt im-config` 单次装,GTK/Qt IM 模块经元包 **`fcitx5-frontend-all`**(覆盖 gtk2/3/4+qt5/6;缺则回退显式集 `gtk3 gtk4 qt5 qt6`,best-effort)——**模块缺失则 GTK/Qt 应用回退 XIM、候选框定位错乱漂到屏角**,故必装(`--no-install-recommends` 是 lib 默认须显式装);apt 候选探测 `_rime_apt_available` 用 **`LC_ALL=C apt-cache policy`** 避免本地化文案误判(同 `sudo_passwordless` 的「按稳定文本/退出码而非可翻译文案判断」铁律);`install` 检测到模块缺失即补装、**旧的坏安装重跑自愈**,再以**用户态** `im-config -n fcitx5` 选定框架。
    - **受管文件**(整写收敛、改前 `backup_file`):① IM 环境 `~/.config/environment.d/ubuntu-setup-rime.conf`(`GTK_IM_MODULE`/`QT_IM_MODULE`/`XMODIFIERS`=fcitx,需重新登录);② RIME patch `~/.local/share/fcitx5/rime/default.custom.yaml`(patch `schema_list`+`"menu/page_size"`;**rime-ice 启用时不写 `schema_list`**,交给其自带 `default.yaml`);③ **fcitx5 登录自启** `~/.config/autostart/org.fcitx.Fcitx5.desktop`——**Wayland 必需**:`im-config` 仅在 **X11** 启动 IM 守护、fcitx5 包不带 `/etc/xdg/autostart` 项,故不自写则 Wayland(Ubuntu 默认会话)登录后无人启动 fcitx5、退回 IBus(这是「装了 RIME 重启后切回原输入法」的根因)。`do_install` 幂等(已装仅收敛、**不覆盖**已存在的 `default.custom.yaml`);`do_remove` **保守**:仅 `apt_remove fcitx5-rime`,保留核心/env.d/用户 RIME 数据并打印彻底卸载指引。
    - **curated 内置方案**(`fcitx5-rime` 自带 `rime-data`,零额外下载;一行说明经 `UI_LANG` en/zh/ja 本地化,本地表 `RIME_I18N`,**方案名不译**):`luna_pinyin_simp`〔简体优先〕`luna_pinyin` `luna_pinyin_fluency` `double_pinyin_flypy`〔小鹤双拼〕`double_pinyin` `bopomofo` `wubi86` `wubi_pinyin` `cangjie5` `stroke` `terra_pinyin`,外加 `add-schema <id>` 接受任意已有数据的方案。**雾凇拼音 rime-ice**(opt-in、纯用户态、无 sudo):`install-rime-ice` 需 `git`,`git clone --depth 1 iDvel/rime-ice` 到 kit 自管克隆,复制进 RIME 用户目录并把路径记入清单 `~/.config/ubuntu-setup/rime-ice.manifest`(覆盖前 `backup_file`),置 `RIME_ICE=1` 重写 `default.custom.yaml`;`remove-rime-ice` 按清单精确删除(拒绝绝对/`..` 路径)+ 删克隆 + 复位 + 恢复 `schema_list`。
    - **模糊音(模糊音)**(opt-in、默认空、纯用户态):是**每方案的 `speller/algebra`** 特性(非全局),故**不写 `default.custom.yaml`**,而是写进**各活跃拼音方案**的 `<schema>.custom.yaml`——rime-ice 启用→`rime_ice`,否则→`luna_pinyin` 系全拼方案(`luna_pinyin_simp`/`luna_pinyin`/`luna_pinyin_fluency`/`terra_pinyin`;双拼/五笔/仓颉等非全拼**故意不作目标**,derive 规则只对全拼有意义),经 **`speller/algebra/+` 追加**(不复制方案原 algebra,rime-ice/luna 升级也不失效)。curated 10 对 `zh_z`〔平翘舌〕`n_l` `f_h` `l_r` `g_k` `in_ing` `en_eng` `an_ang` `ian_iang` `uan_uang`(键名不译、一行说明 en/zh/ja 本地化于 `RIME_I18N[*:fz_<key>]`),各映射到 RIME `derive` 规则(对照 rime-ice schema 内置的注释块)。**标记式归属**:只覆盖/删带 kit 标记(首行 `_RIME_FUZZY_MARK`)的文件,用户自有 `<schema>.custom.yaml` 接管前 `backup_file`+`log_warn`;状态存 `rime.conf` 的 `FUZZY=`,清空或方案/ice 变化时**收敛/清理**我方文件(遍历固定 `_RIME_FUZZY_SCHEMAS`,非目标且属我方者删除);fuzzy 键经 `_rime_fuzzy_valid` 校验,non-target 时 `log_warn` 诚实提示「无拼音方案,模糊音不生效」。
    - **动作**:`deploy`(有图形会话 `fcitx5-remote -r` 重新部署,否则诚实提示);带参 `add-schema`/`remove-schema`〔不允许清空〕/`set-default-schema`〔移到首位〕/`add-fuzzy`/`remove-fuzzy`〔模糊音对〕经 `kit_dispatch`(**不进 `meta.ops`**)且在 `ui()` 交互可达(空格勾方案、`d` 设默认、`a` 加任意方案、page_size 输入、**模糊音勾选区**〔空格切换〕、**静默 GNOME-Wayland 自检弹窗开关**、rime-ice 装/卸、Deploy);`configure` flag `--recommended`〔简体拼音优先+page_size 8+deploy;**不开模糊音**(精度优先)〕/`--schemas`/`--default-schema`/`--page-size <5-10>`/`--fuzzy "对…"`〔`""` 清空〕/`--silence-wayland-diagnose on|off`〔静默 fcitx5 的 **GNOME-Wayland 自检弹窗**(tip `wayland-diagnose-gnome`——登录后约 10s 由 fcitx5 wayland 模块 `selfDiagnose()` 弹出、建议装 kimpanel 扩展;RIME 在普通应用里本就正常,对多数人是噪音):**外科式合并 fcitx5 自有的** `~/.config/fcitx5/conf/notifications.conf` 的 `[HiddenNotifications]` 列表(该文件 fcitx5 自己也写,故**不整写**、只增/删本条 tip、保留其余条目)、语义幂等(状态已对就不写、默认关时全新装根本不碰此文件)、改前 `backup_file`;**默认关、不进 `--recommended`**(保守:不替陌生人机器静默上游诊断)〕/`--deploy`,**无参=保守基线**(`luna_pinyin_simp luna_pinyin`、page_size 5、无模糊音);schema id / fuzzy 键经正则校验(挡 YAML 注入)。
    - **SSH**:RIME/fcitx5 是桌面输入法,SSH/headless 下 install 仍装包并写用户配置(有显示会话后生效),据 `SSH_*`/无 `DISPLAY` 如实提示;检测到 **GNOME+Wayland** 时诚实告知 Mutter 仅原生支持 IBus(fcitx5 覆盖 X11/XWayland 应用),给出「登录选 Ubuntu on Xorg」或「`gsettings` 移除竞争输入源」两条出路(不自动改 GNOME 设置)。
  - **`claude.sh`** 不再只是安装器,而是 **Claude Code 扩展管理器**(组件管理器,同 `zsh.sh` 范式):`install`/`remove`/`status` 管 CLI 本体;装好后**外壳调用官方 `claude` CLI**(MCP、插件)+ 文件式(skills/settings.json)管理五条扩展轴(MCP / 插件市场 / skills / 默认编辑器 / 默认思考级别):
    - **curated 速选**:MCP:`sequential-thinking`/`filesystem`/`memory`/`playwright`〔均 npx/Node〕、`context7`〔http〕;marketplace:`anthropics/claude-plugins-official`/`anthropics/skills`;skill:`pdf`/`docx`/`pptx`/`frontend-design`/`mcp-builder`(取自 `anthropics/skills` 子目录)+ 任意添加;editor:`code`/`cursor`〔均带 `--wait`〕`nvim`/`vim`/`nano`/`micro`/`emacs`〔`-nw`〕`helix` + 任意命令;effort:`low`/`medium`/`high`/`xhigh`/`max`(写 settings.json 顶层 `effortLevel`)。
    - **带参 op** `mcp-add`/`mcp-remove`/`mcp-search`/`marketplace-add`/`marketplace-remove`/`plugin-install`/`plugin-remove`/`plugin-enable`/`plugin-disable`/`skill-install`/`skill-remove`/`set-editor`/`clear-editor`/`set-effort`/`clear-effort`(MCP/插件默认 `--scope user`;**`meta.ops` 仍只 `install,remove`**,带参 op 由 `kit_dispatch` 路由、在 `ui()` 里交互可达——五段勾选界面 MCP/Plugins/Skills/Default editor/Default thinking effort)。
    - **第 4 轴 · 默认编辑器**:Claude Code 的外部编辑器(`Ctrl+G`)读标准 `$EDITOR`/`$VISUAL`、本身无 editor 设置项,故 `set-editor <curated|命令>` 把 `EDITOR`/`VISUAL` 写进 **`~/.claude/settings.json` 的 `env` 块**(Claude-only、不动全局 shell;经 `jq` 改写、改前 `backup_file`、临时文件原子替换、JSON 损坏则拒改),curated 解析为带阻塞参数的命令〔GUI 须 `--wait` 否则进程立即返回、Claude 误判编辑已完成〕、非 curated 原样写、未在 PATH 仅 `log_warn` 仍写;**动态探测**已装编辑器(`have_cmd` 探测命令),`ui()` 里已装可选(当前值打 ✓)、未装灰显标 `(not installed)`,另有「设置自定义编辑器…」与「清除(回退 shell `$EDITOR`)」;`clear-editor` 删 `env.EDITOR/VISUAL`、`env` 空则删之。说明 en/zh/ja 本地化(`CLAUDE_I18N`,编辑器名不译)。
    - **第 5 轴 · 默认思考级别**:Claude Code 的推理强度(`/effort` 级别 `low`/`medium`/`high`/`xhigh`/`max`)持久化在 **`~/.claude/settings.json` 顶层的 `effortLevel`**,故 `set-effort <级别>` 写它后**新 session 即从该级别起步**(等价于跑一次 `/effort` 并固化);同编辑器轴经 `jq` 改写、改前 `backup_file`、临时文件原子替换、JSON 损坏则拒改,但级别是**枚举**(`_claude_effort_valid` 先校验,挡 JSON 注入/无效值,不像编辑器轴接受自由命令);`max` 照写但 `log_warn` 它可能不跨 session 持久(Claude Code 已知限制)、指向 `CLAUDE_CODE_EFFORT_LEVEL=max`;`clear-effort` 删 `effortLevel`(回退各模型内置默认、等价 `/effort auto`);`ui()` 里 5 级可选(当前打 ✓)+「清除」行,说明 en/zh/ja 本地化(`CLAUDE_I18N`,级别名不译)。`jq` 经共享的 `_claude_need_jq`(由编辑器轴更名通用化)按需补装。
    - **安全/幂等**:全程用户态、**拒绝 sudo 包裹**、幂等(`claude mcp get`/`plugin list`/目录探测/settings.json env 探测)、skill 删除前 tar 备份且 kit 自有 skill〔`ubuntu-install`/`zsh-setup`/`claude-extensions`〕受保护;`jq` 用于 `mcp-search`(缺失则降级)与编辑器轴/思考级别轴的 settings.json 改写(缺失则 `apt_install jq`);**绝不自动装 Node**〔指向 `swkit node install`〕、绝不 `sudo npm`。
  - **`mattpocock-skills.sh`** 是 **Matt Pocock skills 合集([`mattpocock/skills`](https://github.com/mattpocock/skills))的多 agent 安装器**(ai)。与 `claude.sh` 的 skill 轴(仅 Claude Code、自 clone+copy)不同,它包一层**开放 agent-skill CLI** `skills`(`vercel-labs/skills`,`npx -y skills@latest`),把**同一份 `SKILL.md`** 装进**任意 agent**(curated:`claude-code`/`codex`/`cursor`/`opencode`/`gemini-cli`/`windsurf`/`github-copilot`;CLI 支持 70+)——"不止配 Claude、还能配其他终端"正是它独立成脚本的理由。
    - **读写分离**:**读**(`status`/`ui` 状态)直接扫各目标 agent 的全局 skills 目录(`~/.claude/skills`、`~/.codex/skills`…,纯 fs、离线、瞬时,**不**每帧 spawn npx;为此维护一张仅 curated 7 个 agent 的 `agent→全局路径` 表);**写**(装/卸/更新)调 `skills add/remove/update -g -y`。
    - **curated skill** = 仓库 `.claude-plugin/plugin.json` 钦定的 **17 个**(engineering 12 + productivity 5;skill 名==目录名==`--skill` 值,一行说明 en/zh/ja 本地化、**名不译**),UI 里可 `按名添加`任意 skill(覆盖 misc/personal/in-progress;非 curated 的记入 conf 的 `EXTRA_SKILLS` 作可再装行)。
    - **目标 agent 集**:存 `~/.config/ubuntu-setup/mattpocock-skills.conf`(`AGENTS=`/`EXTRA_SKILLS=`,grep 读不 source),首次默认=检测到的(至少 `claude-code`);`set-agents`/`add-agent`/`remove-agent` 维护。
    - **op**:`meta.ops=install,remove,configure,update`(`install` 无参=装 17、`remove` 无参=卸所有受管、`configure` flag `--recommended`/`--agents`/`--skills` 无参=保守基线、`update`=`skills update`);**带参 op**(不进 `meta.ops`、`ui()` 可达)`add-skill`/`remove-skill`/`set-agents`/`add-agent`/`remove-agent`/`list`。
    - **安全/幂等**:全程用户态、**拒绝 sudo 包裹**(`_mps_user_paths` 认 `SUDO_USER`)、**绝不自动装 Node**(`_mps_node_gate` 缺则指向 `swkit node install`、只挡写,读仍可用)、绝不 `sudo npm`;`remove` **永不 `--all`**(避免误删其他源的 skill),只删目标集里指定/受管的 skill;agent/skill 名正则校验挡注入;状态实时由 fs 扫描得出(无自建 manifest)。`ui()` 两区(Target agents / Skills 按类目)+ Actions(装推荐 17/更新/卸全部)+ Node 缺失横幅,读走 fs、改走 `ui_run -- "$0" <op>`。
  - **`vscode.sh`** 是 **VS Code 编辑器的安装器**(common):渠道 **Microsoft 官方 apt 源**(经 `add_apt_keyring`/`add_apt_source` 把 key 放 `/etc/apt/keyrings` + `signed-by=`,**绝不** `apt-key`;加源后清 `_KIT_APT_UPDATED` 哨兵强制刷新再装,装后随系统 `apt upgrade` 更新)→ 回退 **snap `--classic`**;`remove` 按 dpkg/snap 来源择一卸载、保留 `~/.config/Code`。VS Code 是桌面 GUI 应用,SSH 下据 `SSH_*` 提示用 **Remote-SSH / `code tunnel`**(窗口在有显示器的机器上)。
  - **`cursor.sh`** 是 **Cursor(AI 代码编辑器)的安装+更新管理器**(common):Cursor 无 apt 源,经其下载 API(`cursor.com/api/download?platform=linux-x64|arm64&releaseTrack=stable`,grep 取 `debUrl`、无需 jq)取**官方 `.deb`**,经 `apt_install <本地 .deb>` 装(解依赖、可 `apt remove cursor` 干净卸载)——同 `ghostty.sh` 的「vendor `.deb`」渠道。因是一次性 `.deb`(无源拉更新),另有 **`update`** op 重取并装最新 `.deb`。桌面 GUI 应用,SSH 据 `SSH_*` 给 Remote-SSH/隧道提示。
  - **`go.sh`** 是 **Go 开发环境管理器**(runtime,**像 `zsh.sh` 一样按组件管理**;上层状态全部实时可观测、无 pref store):`install`/`remove` 管 apt **`golang-go`** 基础工具链(元包拥有 `/usr/bin/go|gofmt` 符号链接,`apt remove` 后 `go` 离开 PATH、幂等;**不**取 go.dev tarball/不加 vendor 源——换基础渠道是日后取舍)。其上管三类组件:① **GOBIN 上 PATH**——把 `$(go env GOPATH)/bin`〔默认 `~/go/bin`〕经标记行写入 shell rc(改前 `backup_file`,`--ensure-path on|off` 增删),使 `go install` 的工具可运行;② **模块代理 GOPROXY**(Go 的"包管理"是内置 `go mod`,代理是其取包途径;curated 预设 `default`/`goproxy.cn`/`goproxy.io`/`direct` + 任意值,经 **`go env -w/-u`** 写 `~/.config/go/env`、用户态不 sudo;`set-proxy <名|url>`);③ **curated 开发工具**(`gopls`/`goimports`/`staticcheck`/`gofumpt`/`dlv`/`golangci-lint`,各经 **`go install <path>@latest`** 装入 GOBIN、用户态不 sudo;`tools`=装推荐集、`update-tools`=对已装工具重装 `@latest`、`add-tool`/`remove-tool` 单个增删;一行说明 en/zh/ja 本地化)。`configure` flag:`--recommended`〔PATH + 推荐工具〕/`--goproxy`/`--tools`/`--ensure-path`,无参=保守基线(仅把 GOBIN 上 PATH)。配置/工具/代理动作**拒绝 sudo 包裹**(`_go_user_guard`,认 `SUDO_USER` 解析真实 home/rc),仅 apt 经 `sudo_run` 逐命令提权。`ui()` 把 PATH 开关、GOPROXY 选择器、工具勾选清单、「Apply recommended」、卸载做成组件管理界面。
  - **`python.sh`** 是 **现代 Python 开发环境管理器**(runtime,**按组件管理**;Ubuntu 自带 `python3`,故**绝不**碰 `python3` 本身):`install`/`remove`/`status` 管 apt 系统开发层(`python3-pip`/`python3-venv`/`python3-dev`;`status` 以「`python3` + `python3 -m pip` 可用」为「已装」判据,`remove` 只卸 pip/venv/dev)。其上管现代工具链:① **uv**(Astral 的 Rust 包/项目/Python 版本/工具统一管理器,取代 pip/venv/virtualenv/pipx/poetry/pyenv):经**官方脚本** `curl -LsSf https://astral.sh/uv/install.sh | sh` 装入 `~/.local/bin`(用户态、显示 URL、**绝不 sudo**),连同 shell 补全 + `~/.local/bin` 上 PATH;`install-uv`/`update-uv`〔`uv self update`〕/`remove-uv`〔按官方步骤完整卸载:`uv cache clean` + 删 `uv python dir`/`uv tool dir` + 删 bin + 清受管 rc 行;**会删 uv 管理的 Python/工具,故仅显式 op 且 ui 内二次确认**〕;② **uv 工具**(`ruff`/`ty`/`mypy`/`ipython`/`pre-commit`,各经 **`uv tool install`** 隔离安装,等价现代 pipx;`tools`=装推荐〔`ruff`〕、`update-tools`=`uv tool upgrade --all`、`add-tool`/`remove-tool` 单个增删;探测 `uv tool list` 时**强制 `NO_COLOR` 并 strip ANSI**,否则带色输出会致工具误判为未装);③ **包索引镜像**(opt-in,默认 PyPI;curated `tsinghua`/`aliyun`/`ustc` + 任意 URL,经 `pip config set/unset` 写 `~/.config/pip/pip.conf` + shell rc 受管 `UV_DEFAULT_INDEX` 行;`set-index <名|url>`);④ **Python 版本管理**(经 uv 管理本机解释器,状态**实时由 uv 安装目录得出、不硬编码版本号**;**绝不碰 `/usr/bin/python3`**):`install-version <ver|latest>`〔`uv python install`,附加装版本化 `python3.X`、**不遮蔽**默认 `python3`;`latest` 经 `_py_latest_available` 从 `uv python list` 解析最新可下载 CPython 再**显式**装,**绝不**用裸 `uv python install`〔无参会读 CWD 的 `.python-version`/已装则不动〕〕/`remove-version`/`list-versions`/`upgrade-versions`〔`uv python upgrade`,patch 升级、uv preview〕,以及 **opt-in 的** `set-default <ver>`〔`uv python install <ver> --default`,装裸 `python`/`python3` 进 `~/.local/bin` 遮蔽 `/usr/bin/python3`——**仅交互 shell**(PATH 序),系统服务/root/绝对 shebang 不受影响;**绝不传 `--force`**(不覆盖用户手放的 exec);护栏同 `remove-uv`:op 本身直接执行+清晰 `log_warn`(headless/LLM 可用),UI 二次确认〕与 `clear-default`〔仅删 uv 自管的裸 `python`/`python3` symlink(经 `uv python dir --bin` 定位、`readlink -f` 落在 `uv python dir` 安装根才删),**保留版本化 exec**、复位系统〕;遮蔽态经 `_py_default_shadowed`(`python3` 真实路径是否落在安装根)探测,`status` 末尾**仅遮蔽时**追加 `python3->X(uv)`,平时加 `pythons N` 计数;已装版本经**文件系统**列出(跳过 uv 的 minor-version 软链目录,避免 3.13 与 3.13.5 重复计)。`configure` flag:`--recommended`〔base + uv + ruff + PATH,**不**装/设任何 uv 版本——新机器零变化〕/`--uv on|off`/`--tools`/`--index`/`--python <ver>`〔确保 uv 后附加装〕/`--default-python <ver>`〔确保 uv 后装+设默认遮蔽〕,无参=保守基线(确保 base + `~/.local/bin` 上 PATH)。uv/工具/配置/版本动作**拒绝 sudo 包裹**(`_py_user_guard`),仅 apt 经 `sudo_run` 提权。`ui()` 把 uv 安装/更新/移除、**Python 版本区**〔默认态行(`→ system`/`→ uv X (shadowing system)`)+已装版本列表(`★ default` 标记、`space` 移除、`d` 设默认)+「装最新/装指定」+`a` 添加+(遮蔽时)`clear-default`+(有版本时)`upgrade-versions`〕、工具勾选清单、索引选择器、「Apply recommended」做成组件管理界面。**机器/项目边界**:只管本机解释器,不纳入 `uv python pin`(写 CWD 的 `.python-version`)/`uv venv`〔逐项目、依赖 CWD,交用户在项目里直接用 `uv`〕。
  - **`java.sh`** 是 **Java(OpenJDK)开发环境管理器**(runtime,**像 `go.sh`/`python.sh` 一样按组件管理**;状态全部实时由 `dpkg`+`update-alternatives` 得出、无 pref store):渠道 **apt OpenJDK**(纯 JDK,**不碰** Maven/Gradle,不加 vendor 源 / 不用 SDKMAN);`install`=基线 **JDK 17**(`openjdk-17-jdk-headless`,含 `javac` 全套、不拉 X/GUI)+ 设默认 + 写 JAVA_HOME;`remove`=卸全部受管 openjdk JDK + 清 JAVA_HOME 行 + 复位 alternatives。curated **{17,21}** + 带参 `add-version <N>`(`^[0-9]+$` 校验 + `LC_ALL=C apt-cache policy` 探测,**不硬编码可装版本**)/ `remove-version <N>`〔卸当前默认且有存活→改指最低存活版 + 重生成 JAVA_HOME,卸空才清行〕。**默认切换的坑**:apt 把 `java`/`javac`/`jar`/… 注册为**独立** alternatives 链,只 `--set java` 会留 `javac` 指向别 JVM(坏 Android 构建),故 `set-default <N>` 用 **`update-java-alternatives` 分组切**(id 从 `--list` 解析)、headless 无 `.jinfo` 时**兜底枚举各链逐个 `--set`**、**切后断言 `java`/`javac` 同主版本**(不一致或缺失即 fail-fast)。受管 **JAVA_HOME** rc 行(`go.sh do_ensure_path` 范式:marker + `backup_file` + `grep -qxF`,`--ensure-java-home on|off`,切默认时重生成)。**`home [<N>]`** 是对外只读查询 op(被 `android.sh` 消费):**stdout 仅打印路径、诊断走 stderr、零 JVM**(绝不 `java -version`)——无参走 `readlink -f /etc/alternatives/java`、带参走 fs glob `/usr/lib/jvm/java-<N>-openjdk-$(dpkg --print-architecture)`(验 `bin/javac`,回退解析 `update-alternatives --query java` 的逐候选 `Alternative:` 段)。`status` 退 0 iff ≥1 受管 JDK(纯 dpkg、`KIT_PROBE_ONLY` 早返布尔、两路径退出码一致零 JVM)。`configure` flag `--recommended`/`--default <N>`/`--ensure-java-home`/`--full-jdk`〔附加装完整 `openjdk-<N>-jdk`〕,无参=保守基线(确保 17 + 默认 + JAVA_HOME)。配置/版本/JAVA_HOME 动作**拒绝 sudo 包裹**(`_java_user_guard`,认 `SUDO_USER` 解析真实 home/rc),仅 apt/alternatives 经 `sudo_run` 提权;`meta.ops`=`install,remove,configure`,带参 op 经 `kit_dispatch` 路由且在 `ui()` 交互可达(版本勾选 /`d` 设默认 /`a` 加任意版本 / JAVA_HOME 开关 / Apply recommended / 卸全部)。
  - **`android.sh`** 是 **headless Android SDK 工具链管理器**(runtime,**按组件管理**;`cmdline-tools`+`sdkmanager` 驱动、**无 Android Studio**〔GUI,日后单独 `android-studio`〕;`$ANDROID_HOME=~/Android/Sdk`、状态实时扫 fs):**单向联动 `android→java`**——任何 `sdkmanager`(JVM)操作需兼容 **JDK≥17**,经 `_android_java_gate` 找(默认≥17 直接用;默认旧但装了≥17→**仅本次** `JAVA_HOME=$("$KIT_SCRIPTS_DIR/java.sh" home <N>)` 进程局部前缀〔结果**判非空**再用——`export VAR=$(失败)` 在 errexit 下静默给空串〕;全无→指路 `swkit java install`、退非 0、**绝不自动装**),gate 在**每个** sdkmanager JVM spawn 前跑(含 `--recommended` 的 `sdkmanager --list`);兼容 JDK 的发现用与 `java.sh` 同款**无界 dpkg glob**(`openjdk-*-jdk*`,不硬编码版本上限);`java.sh` 内不得出现 `android` 引用(grep 强制)。**bootstrap 序**:条件 `apt_install curl unzip` → **块作用域 awk** 解析 `repository2-*.xml`(锁定 `host-os=linux`、跳 alpha/beta/rc/dev 的最新 `<archive>`,**同块**取相对 `<url>`+裸 SHA-1 `<checksum>`+`<size>`,**非三个独立 grep**)→ **`sha1sum`+size 校验后**解压到 `cmdline-tools/latest/`(无需 Java;**该 zip 解压即执行、无 dpkg 闸,故校验是信任边界**)→ Java gate → `sdkmanager`。`install`=cmdline-tools+platform-tools(**许可不隐式**);`--recommended`=+ 动态解析最新 platform/build-tools(单次 `sdkmanager --list` 取值)+ emulator(**headless〔SSH 或无 DISPLAY/WAYLAND〕或无 `/dev/kvm` 剔除**);组件全 opt-in(platforms/build-tools/emulator/system-images〔ABI 默认对齐 `dpkg --print-architecture`、跨 ABI 警告〕/ndk/cmake/scrcpy〔apt〕);带参 `add-package`/`remove-package`〔grammar `^[A-Za-z0-9._-]+(;…)*$` 校验,`scrcpy` 路由 apt〕、`add-platform`/`remove-platform <N>`、`set-mirror <名|url>`。**镜像** opt-in 默认直连:预设 **tencent**(唯一实测可用的 repository2 镜像;tsinghua 版权不镜像、ustc 只镜像 AOSP 源码、aliyun 只有 git 源——均不托管二进制 SDK,故仅留 tencent)+ 任意 URL(校验=**正向 URL 字符集白名单** `^https?://[…]+/$`,挡 `;`/空格/引号/`$`/反引号等元字符;非默认时打**信任披露**——镜像成无签名代码源、无独立信任锚);**bootstrap zip 须显式拼镜像 base**(`SDK_TEST_BASE_URL` 不覆盖它)且 **404 回退直连**。**许可**显式:`accept-licenses`(`yes | sdkmanager --licenses` + 条款 URL + 回显 license id);headless `install` 不自动接受、`--accept-licenses` 才接受、否则放好工具 + 提示完整前置链(`swkit java install` → `accept-licenses` → 装 platform-tools)。`remove` 保守(删 env 块 + kit 装的 cmdline-tools、**保留**下载数据、打印彻底删指引);**`purge`** 破坏性(`rm -rf "${target:?}"`,前置 `_android_user_guard` + 路径护栏〔非空 / 非软链 / `realpath -m` 绝对 / `≠/` / `≠$HOME` / 严格在真实 `$HOME` 下〕+ ui 二次确认;AVD 在 `~/.android/avd/` 不动)。`status` 退 0 iff cmdline-tools 在(纯 fs、`KIT_PROBE_ONLY` 早返、**绝不调 gate、零 JVM**;Java 兼容横幅只由 `java.sh home`/`pkg_installed` 只读信号算)。env 受管**块**(`tmux.sh` 范式:`ANDROID_HOME` 现行规范 + `ANDROID_SDK_ROOT` 弃用别名〔注释写明关系防误删〕+ PATH,`--ensure-path on|off`)。emulator/arm64〔Google Linux 原生件 adb/aapt2/emulator/NDK 仅 x86_64〕/SSH 据 `SSH_*`/`DISPLAY`/`/dev/kvm` 诚实披露。`configure` flag `--recommended`/`--accept-licenses`/`--mirror`/`--ensure-path`,无参=保守基线;SDK 下载/解压/sdkmanager 全程用户态(`_android_user_guard`),仅 curl/ca-certificates/unzip/scrcpy 经 `apt_install` 逐命令提权;`meta.ops`=`install,remove,configure,accept-licenses,purge`,带参 op 经 `kit_dispatch` 路由且在 `ui()` 交互可达(Java 横幅 +「装 Java 17」走 `ui_run -- java.sh install`、组件勾选两区、镜像/许可选择、purge 二次确认)。
- `swkit`——薄启动器:`swkit <软件> <操作>` / `swkit <软件>`(无操作=开该软件 `ui` 界面)/ `swkit ui`(`ui_catalog` 总目录)/ `list`(按类别分组、标 `[installed]`,走 `kit_cache_fill` 缓存、热则近瞬时)/ `search <term>`(走 `kit_meta_warm` 缓存)/ `warm`(`kit_cache_warm` 预热,供 LLM/cron/脚本预加载)/ `cache clear` / `--no-cache`(全局前缀,置 `KIT_NO_CACHE=1` 旁路缓存)/ `help`。
- `skills/ubuntu-install/SKILL.md`、`skills/zsh-setup/SKILL.md`、`skills/claude-extensions/SKILL.md`——「如何用脚本」守则(挑脚本/选项、品味取舍、锁定安全;**不教 LLM 写/演进脚本**)。
- `README.md` + 本文件。

设计蓝图见 `docs/superpowers/specs/2026-06-12-script-collection-llm-evolution-design.md`(**注意:其中「LLM 在运行时演进脚本」一支已弃用**——现在脚本只在本仓库维护、纯拷贝部署;该 spec 的脚本集合 / 统一接口 / 安全契约部分仍有效)。**现代化 TUI + 脚本自有 UI 重构**(`lib/ui.sh`、`ui()` 约定、丢弃 whiptail、bootstrap 退化为启动器)见 `docs/superpowers/specs/2026-06-13-script-owned-tui-ui-refactor-design.md`。**OMZ 最佳实践 + Nerd Fonts**(`fonts.sh`、zsh 的 OMZ 原生插件/设置)见 `docs/superpowers/specs/2026-06-13-omz-best-practices-and-nerd-fonts-design.md`;**Claude Code 扩展管理器**(`claude.sh` 的 MCP/插件/skill 三轴)见 `docs/superpowers/specs/2026-06-14-claude-code-extension-manager-design.md`;**默认编辑器(第 4 轴)**(`claude.sh` 经 settings.json `env` 设 `EDITOR`/`VISUAL`、动态探测已装编辑器)见 `docs/superpowers/specs/2026-06-19-claude-default-editor-design.md`。前一份 TUI-catalog spec(`2026-06-12-bootstrap-software-manager-tui-design.md`)**部分被取代**:其「类别 → 软件 → 操作」三级导航思想保留(现为「类别 → 软件 → 该软件自己的 `ui()`」),「装 / 配逻辑以 `sw_<key>_*` 内联函数写死在 bootstrap.sh 内」迁到 `scripts/`,而其 whiptail 渲染被 `lib/ui.sh` 取代。放弃数据驱动 catalog 引擎、内联函数迁成脚本集合、whiptail 迁成自绘 TUI 的 pivot 来龙去脉保留在 git 历史中。

## 构建 / 校验命令

无编译工具链。校验:

```bash
# 语法检查(必过)——bootstrap、三个库、每个脚本、launcher
for f in bootstrap.sh lib/common.sh lib/cache.sh lib/ui.sh swkit scripts/*.sh; do bash -n "$f"; done

# shellcheck(保持零告警)。所有文件都 source lib(common.sh 再 source cache.sh、ui.sh),
# 统一带 -x 跟随 source 指令、用 SCRIPTDIR 让 source-path 相对每个文件解析:
shellcheck -x --source-path=SCRIPTDIR swkit scripts/*.sh lib/common.sh lib/cache.sh lib/ui.sh bootstrap.sh

./bootstrap.sh --help                      # 用法说明
```

`bootstrap.sh` 设计为可 `source`(末尾 `[[ "${BASH_SOURCE[0]}" == "$0" ]]` 守卫只在被执行时跑 `main`),便于对 `have_tty`、依赖检查等函数做函数级单测。`scripts/*.sh` 自身 `source` lib,所以脚本侧的静态检查走上面的 `shellcheck -x --source-path=SCRIPTDIR` 形式;脚本契约则用其自带子命令测(`<script> meta` 字段齐全且 `ops` 与实现一致、**`ops` 不含 `ui`**、`<script> status` 可独立运行、`<script> help` 不炸、**`<script> ui` 在无 TTY 下打印指引退 0**)。富屏渲染无法 headless 自动测,可用伪终端冒烟(`printf 'q' | TERM=xterm-256color script -qec '<script> ui' /dev/null`,确认渲染不崩、`q` 干净退出、终端复原)。

## 领域约束

本项目特有、实现 `bootstrap.sh` / `lib/common.sh` / `lib/ui.sh` / `scripts/*.sh` / 两个 `SKILL.md` 时需贯彻的关键点;本节即唯一来源。

- **目标是刚装好的 Ubuntu**:不能假设系统已预装基础系统以外的工具(最小 Server 镜像可能连 `curl`/`git` 都没有);apt 操作需提权与免交互(`DEBIAN_FRONTEND=noninteractive`)。但**不能假设机器是纯净的**——用户可能在既有环境上跑,改任何东西前先查、先备份、失败安全。
- **仅 Ubuntu/Debian**:不做多发行版支持。

以下为**不可妥协项**。它们**同时**作为 `lib/common.sh` 的代码强制、与本文件下方「编写契约」(给在仓库里写/改脚本的**贡献者**)存在。**漂移警告**:改 lib 的安全语义,必须同步本文件的契约散文;反之亦然——两处描述的是同一份底线,会各自漂移。注意:**旧的「每软件 configure 逻辑双写在 bootstrap bash 与 skill 散文、改一处必查另一处」已被消除**——configure 的唯一来源是脚本(`do_configure`),skill 只讲「如何用脚本」,不再平行实现。

- **①幂等查活系统**:每个操作以观察真实系统为前提(`have_cmd` / `pkg_installed` 查 dpkg 恰为 `install ok installed` / 版本输出),绝不依赖记录的标志位。落地:lib 的探测原语 + 每脚本的 `status` 闸门(`do_install`/`do_remove` 先跑 `status`,已装则报版本跳过、未装则提示跳过)。`status` 退出码 0 当且仅当已装 / 已生效。重跑 `bootstrap.sh` 或任一脚本第二次都是安全 no-op。

- **②绝不整体 root,逐命令 sudo**:脚本以普通用户身份跑,只对确需 root 的单条命令逐命令提权。落地:lib 的 `sudo_run CMD...` 是**唯一**提权途径——`EUID==0` 直接执行;sudo 免密则 `sudo CMD`;有可写 `/dev/tty` 则 `sudo CMD`(正常弹密码);否则(LLM 的无 TTY shell)打印「请你自己执行:sudo CMD」并返回 `RC_NEED_SUDO=97`,脚本因 `set -e` 而停。`apt_install`/`apt_remove` 等都经它提权。官方 native installer 与 `npm install -g` 一律以当前用户身份跑;**`sudo npm install -g` 严禁**——`npm_global_writable` 是判定原语(prefix 不可写返回非 0);仅 npm 通道的脚本(`codegraph`/`trellis`,及 `codex`/`claude` 的 npm 方式)装前调 `npm_ensure_user_prefix`:prefix 为系统默认(`/usr`、`/usr/local`)时以**用户态**写 `~/.npmrc` 把前缀改到 `~/.npm-global`〔专属目录,避开 native 安装器(Claude/Codex)的 `~/.local/bin` 落点,防 `claude update` 误报残留〕(`npm config set prefix`,无 sudo、可 `npm config delete prefix` 还原)再装,**自定义且不可写**的前缀则**不动**、退回报错——把「提示用户手改」升级为「默认就走安全路径」,绝不伸手 sudo;且**被 sudo 整体包裹时(`EUID==0` 且有 `SUDO_USER`)直接拒绝**,提示以本人身份重跑(npm 全局安装绝不以 root 跑)。`sudo_run` **绝不** echo/pipe/here-string 密码进 `sudo -S`、**绝不**存密码、**绝不**自写 NOPASSWD 规则。被 sudo 整体包裹时用 `SUDO_USER` 解析真实用户 home/身份(见 `zsh.sh`/`docker.sh` 里的 `user="${SUDO_USER:-$(id -un)}"`),绝不信任 `~`/`$HOME`。
  - **sudo 密码(两阶段)**:阶段 A 的 `bootstrap.sh` 由用户在自己终端直接运行,有真实 TTY,sudo 正常弹提示;TUI 的「设置」页有**开关**(`ui_confirm` 模态;非富终端回退 `/dev/tty` 文本 `[Y/n]`;headless 时为一次性提示)开 / 关 `/etc/sudoers.d/ubuntu-setup-llm`(`<user> ALL=(ALL) NOPASSWD:ALL`)——因为阶段 B 由 LLM 经自己的 Bash 工具跑 sudo,**该环境无交互 TTY、无法输密**,不预先免密则 LLM 装不了软件。**特权写入经 `ui_run`**(它退出备用屏,sudo 密码提示走真实终端——比旧的进度条管道更正,旧方案靠 preauth 绕开 pipe 无法输密)。开关只走 UI、**不设命令行 flag**(见用户记忆:toggle 用交互提示而非 flag),显示当前状态且可双向切换。`have_tty` 须**真正 open `/dev/tty`** 探测(`true </dev/tty` 且 `true >/dev/tty`):设备节点存在不等于可打开,无控制终端时 open 会 ENXIO,只看 `-r`/`-w` 权限位会误判为交互、把 headless 误导进 TUI。`sudo_passwordless` 用 `sudo -n true` 的**退出码**判断,**不看文案**(classic sudo 与 sudo-rs 文案不同且会被本地化)。此规则在 `bootstrap.sh`(开关 UI)、`lib/common.sh`(`sudo_run`/`kit_have_tty`/`sudo_passwordless`)、`lib/ui.sh`(`ui_confirm`/`ui_run` 的渲染与退屏)、与两个 `SKILL.md` 的契约散文里各自落地——改一处必查其余。

- **③非交互 apt**:每条 apt 命令都免交互。落地:lib 的 `apt_install`(`env DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends`)/ `apt_remove`(`remove` 而非 `purge`,保留用户配置)/ `apt_update_once`(进程内首次才真跑,用哨兵);脚本不自己写裸 `apt-get`。绝不在无桌面 server 上卡 debconf 提示。

- **④fail-fast、可续跑、无回滚**:脚本各自 `set -Eeuo pipefail`,首错即停;补救方式是重跑(幂等保证安全)。改配置文件前先 `backup_file`(时间戳副本是唯一的"撤销");`append_once`(`grep -qxF`)保证重跑不累积。**例外**:headless 路径保持严格 fail-fast;但交互界面里每个状态变更经 `ui_run "<标题>" -- <script> <op>` 单独跑(子进程),`ui_run` **退出备用屏**直接展示真实输出(apt/sudo 提示正常)、`tee` 到 `~/.cache/ubuntu-setup/` 日志、显示 `✓/✗`、回车再回屏。**操作失败(脚本非 0,含 `RC_NEED_SUDO`)不杀界面循环**——`ui_run` 返回退出码供调用方记录、点名日志路径、回菜单,靠幂等重跑补救;`ui_begin`/`ui_end` 的 `trap … EXIT INT TERM` 保证任何崩溃都复原终端(备用屏/光标/`stty`)。(旧的 `whiptail --gauge` 子 shell 与 `trap '' PIPE` 已随 whiptail 一并移除。)

- **⑤kit 与 skill 都是面向用户机器的产品资产**:`scripts/` + `lib/` + `swkit` 与 `skills/` 都部署到最终用户机器(陌生人机器),同一底线:渠道保守(渠道优先级 `apt` → vendor apt repo(`add_apt_keyring`/`add_apt_source`,keyring 进 `/etc/apt/keyrings` + `signed-by=`,**绝不** `apt-key`)→ snap → 官方 vendor 脚本(显示 URL)→ 手动二进制(最后手段))、先计划后执行、显式验证。它们必须在作者看不见的机器上站得住。

- **⑥目录加载零卡顿(缓存即 UI)**:`ui_catalog` 用 stale-while-revalidate——`_ui_catalog_scan` 从 `lib/cache.sh` 缓存**零 fork 瞬时首帧**渲染(meta/status 经 `kit_meta_into`/`kit_status_read` 直接读缓存文件填全局、不捕获子 shell;热 ~4ms、冷 ~40ms),后台并行 `kit_probe_status` 重探,`ui_read_key` 超时轮询收割、status 到达即重绘(三态 badge:已装/未装/探测中);`r` 硬刷新、返回子界面后精准失效该脚本。`meta` 按文件 mtime 永久缓存(18 个 meta 全静态),`status` 按软 `KIT_STATUS_TTL` 缓存——**陈旧只影响显示不影响行为**(真实 op 永远跑脚本本体、其 status 闸门现场复检)。重型 `status` 经 **`KIT_PROBE_ONLY`** 约定(cache 探测时设)只算安装布尔、跳过昂贵详情(版本/工具枚举),两条路径退出码必须一致。**预加载**:bootstrap 进 TUI 后台 `kit_cache_warm`、`swkit warm` 显式预热、磁盘缓存跨 `swkit` 调用复用。此约定同时落在 `lib/cache.sh`/`lib/ui.sh`/`swkit`/`bootstrap.sh`/`scripts/TEMPLATE.sh`/本文件——改一处必查其余。

- **编写契约(给在仓库里写 / 改脚本的贡献者)**:`cp scripts/TEMPLATE.sh scripts/<key>.sh`,保留结构(`#!/usr/bin/env bash`、`set -Eeuo pipefail`、定位并 `source` lib、定义 `meta`/`status`/`do_install`/`do_remove`[/`do_configure`][/`ui`]/`usage`、末尾 `kit_dispatch "$@"`);**一切提权 / 包 / 文件操作走 lib helper**,绝不裸 `sudo`/`apt-get`/`apt-key`/`sudo npm`;`meta` 的 `ops` 必须**恰好**列实现了的操作(**`ui` 是入口模式、不进 `ops`**),`category ∈ essentials|common|ai|runtime`(其余归 `other`);幂等(`do_install`/`do_remove` 闸在 `status` 上);改配置文件前备份、追加前 grep;先计划后执行、fail-fast、不回滚(重跑即补救,故须幂等);测试 `bash -n` → `shellcheck -x` → 脚本自身 `status`(装前 / 装后)→ `ui` 在无 TTY 退 0 →(伪终端冒烟富屏)→ 确认后真跑;改动提交到**本仓库**(普通 git 流程),`git commit` 留清晰信息——因 kit 原地从 clone 跑,改完即生效(`swkit` 直指 clone);只有改 `skills/` 才需重跑 `bootstrap.sh` 刷新 `~/.claude`/`~/.codex`。
  - **`ui()`(交互界面,可选但鼓励)**:省略则 `kit_dispatch ui` 用 `ui_default_menu`(由 `meta.ops` 合成)兜底;手写则用 `lib/ui.sh` 原语——`if ! ui_supported; then ui_default_menu; return 0; fi` 起手、`ui_begin`/`ui_end` 包住循环、**每个状态变更经 `ui_run "<标题>" -- "$0" <op> [args]`**(退屏跑、可见输出+日志、回屏后重载状态),配合 `ui_pick`/`ui_confirm`/`ui_input`/`ui_notify`/`ui_badge`/`ui_header`/`ui_footer`/`ui_row`/`ui_read_key`。样板:`scripts/zsh.sh`(旗舰)、`scripts/TEMPLATE.sh`(注释样例)。**UI 契约漂移**:`ui()` 约定 / `ui` 非 op / 三档终端降级 同时落在 `lib/ui.sh`、`TEMPLATE.sh` 与两个 `SKILL.md`——改一处必查其余。
  - `configure` **默认**(无参)是**保守 headless 安全基线**;但脚本**可以把该软件的常用方案作为显式 opt-in 覆盖进来**(作者裁量)。例:`zsh.sh` 是一个**组件管理器**——框架(Oh My Zsh)/提示符/各插件可**独立装/卸/增/删**,状态记在 `~/.config/zsh/ubuntu-setup.conf`,每次改动整体重生成受管 drop-in `~/.config/zsh/ubuntu-setup.zsh`(收敛、不覆盖用户 `~/.zshrc`)。动作:`install-omz`/`uninstall-omz`、`add-plugin <名|git-url>`/`remove-plugin <名>`(kit 自管插件:已知集 + 任意 git)、`prompt <名>`、锁定安全的 `default-shell`、`configure` 一次性全量;**OMZ 原生插件**(独立于 kit 插件的另一维度,经 OMZ 的 `plugins=($OMZ_PLUGINS)` 启用)用 `add-omz-plugin <名>`/`remove-omz-plugin <名>`(curated:`git sudo extract colored-man-pages command-not-found docker docker-compose kubectl z`,加任意已存在于 `~/.oh-my-zsh/plugins` 的;**故意不含** autosuggestions/syntax-highlighting——那是 kit 自管插件,避免双重加载);**OMZ 设置逐项可配**用 `omz-setting <key> <value>`(`update`/`magic`/`untracked-dirty`/`correction`/`wait-dots`/`hist-stamps`,默认是保守的性能/非交互安全最佳实践,如 `update=disabled` 因 kit 用 git 管更新)。无参动作进 `meta.ops`,带参的 `add-plugin`/`remove-plugin`/`prompt`/`add-omz-plugin`/`remove-omz-plugin`/`omz-setting` 由 swkit/LLM 调用,**且在 zsh 的 `ui()` 里交互可达**(空格勾插件、`a` 加 git 插件、提示符子菜单、OMZ 开启时多出「Oh My Zsh plugins」勾选区 +「＋ add」行 +「Oh My Zsh settings」可调区——旧的「带参动作进不了 TUI」限制已不复存在)。`docker.sh` 的 `configure` 加 `docker` 组 + 起服务,`git.sh` 的 `configure` 设全局 user.name/email。`claude.sh` 同为**组件管理器**:`install`/`remove`/`status` 管 CLI 本体,装好后**外壳调用 `claude` CLI**(MCP、插件)+ 文件式(skills/settings.json)管五轴,带参 op(`mcp-add`/`marketplace-add`/`plugin-install`/`skill-install`/`set-editor`/`clear-editor`/`set-effort`/`clear-effort` 等,MCP/插件默认 `--scope user`)由 swkit/LLM 调用且在 `ui()` 五段勾选界面里可达,用户态、拒绝 sudo 包裹、curated+任意添加、幂等;**默认编辑器轴**把 `EDITOR`/`VISUAL` 写进 `~/.claude/settings.json` 的 `env`(Claude-only,经 `jq`),**默认思考级别轴**把 `/effort` 级别写进 `settings.json` 顶层 `effortLevel`(枚举校验,`max` 提示可能不持久)。**唯一来源仍是脚本**;skill(`zsh-setup`、`claude-extensions`)负责**帮用户选**(品味、Nerd Font/SSH 取舍、MCP/插件的 token 成本与运行时/密钥/作用域、锁定安全纪律)与用脚本的现有动作做 bespoke 定制(**不在用户机器上写/改脚本**)。

- **安全考量**:脚本会在陌生人机器上跑 `sudo`——开源产品,这是真实攻击面。缓解:① lib 原语让安全写法成为默认、不安全写法无原语支持;② skill 强制「先计划后执行 + 用户确认」;③ 脚本都在**本仓库**里、经正常 git 评审后才发布,`git diff` 可见可审、可回滚;④ 绝不自动写 NOPASSWD;⑤ LLM 在用户机器上只**运行**已发布的脚本,不在运行时生成/改写脚本。
